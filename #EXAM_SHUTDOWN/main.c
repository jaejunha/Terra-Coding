#include <stdio.h>
#include <Windows.h>

void main()
{
  system("shutdown /s /t  0"); // this line means shutdown command.
  // if you compile this file as release version, you can use the file as executable file.

  return;
}